元素的显示方式有：
1、行内元素：
   主要的行内元素有：<a>、<em>、<ins>、<span>、<strong>等跟文字相关的。
2、块元素：<h1>、<p>、<div>、<ul>、<ol>、<li>、<dt>。
3、行内块元素：<input><img><td>。


行内元素：和相邻行内元素在一行上，不能设置宽高，宽度由内容决定，只能容纳文本和其他行内元素。
块元素：独占一行，可以设置宽高，可以容纳其他块元素和内联元素。
行内块：和相邻行内元素在一行上，但可以设置宽高。